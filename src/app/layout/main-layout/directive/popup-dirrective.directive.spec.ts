import { PopupDirrectiveDirective } from './popup-dirrective.directive';

describe('PopupDirrectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupDirrectiveDirective();
    expect(directive).toBeTruthy();
  });
});
