import { VectorBgDirective } from './vector-bg.directive';

describe('VectorBgDirective', () => {
  it('should create an instance', () => {
    const directive = new VectorBgDirective();
    expect(directive).toBeTruthy();
  });
});
